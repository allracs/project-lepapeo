@model IEnumerable<LePapeo.Models.ReservaViewModel>
@using LePapeoGenNHibernate.CEN.LePapeo

@{
    ViewBag.Title = "ReservasProximas";
    Layout = "~/Views/Shared/_Layout.cshtml";

    RestauranteCEN resview = (RestauranteCEN)ViewData["resview"];

}

<nav class="navbar navbar-inverse" style="margin-top:3em;">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="MiPerfil">Mi perfil</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="ReservasProximas">Reservas próximas</a></li>
            <li><a href="ReservasAnteriores">Reservas anteriores</a></li>
            <li><a href="MisOpiniones">Mis opiniones</a></li>
            <li><a href="MisDatos">Mis datos</a></li>
            <li><a href="CambioPass">Cambiar contraseña</a></li>
            <li><a href="EliminarCuenta">Eliminar cuenta</a></li>
        </ul>
    </div>
</nav>

<table class="table">
    <tr>
        @*<th>
                @Html.DisplayNameFor(model => model.idusuario)
            </th>*@
        <th>
            Restaurante
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comensales)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.finalizada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_hora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_solicitud)
        </th>
        @*<th></th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.idusuario)
                </td>*@
            <td>
                @resview.ReadOID(item.idrestaurante).Nombre
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.comensales)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.finalizada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fecha_hora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fecha_solicitud)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.id })
                </td>*@
        </tr>
    }

</table>
